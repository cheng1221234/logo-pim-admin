version: '3.8'

services:
  app:
    build: .
    container_name: logo-pim-admin
    restart: always
    ports:
      - "3005:3005"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
      - ./logs:/app/logs
    environment:
      - NODE_ENV=production
      - PORT=3005
